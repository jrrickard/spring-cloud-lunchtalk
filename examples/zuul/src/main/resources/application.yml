info:
  component: Zuul Server

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

eureka:
  instance:
    preferIpAddress: true

#zuul:
#  ignoredServices: '*'
#  routes:
#    users:
#     path: /hello/**
#     serviceId: HELLO

server:
  port: 8765

logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG